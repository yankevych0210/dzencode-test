{
    "name": "dzencode-test-root",
    "private": true,
    "scripts": {
        "build": "cd client && npm ci --legacy-peer-deps && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}